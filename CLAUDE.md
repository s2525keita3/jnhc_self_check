# このリポジトリで作業するときの前提

## 中身

| フォルダ | 内容 |
|---|---|
| `kaigo_kensaku/` | 厚労省 介護サービス情報公表システム → Excel の取得ツール（Python / Selenium / customtkinter） |
| `jnhc_pwa/` | 訪問看護ステーション向けのPWA |
| `john_ai/` | コンサルティング支援 |
| `docs/` | 開発の手引き |

## 公開サイトから情報を取るツールを作る・直すとき

**着手前に `docs/スクレイピングツール開発の手引き.md` を読むこと。**

前回（`kaigo_kensaku/`）はツール本体の20コミットのうち15件が不具合の修正で、
「実物のHTMLを見ずに推測で書いた」ことが原因のものが、確認できるだけで12件
あった。同じ失敗を繰り返さないための手順とチェックリストが手引きにある。

特に効くのは次の3つ。

1. **実物のHTMLを1枚もらうまで、抽出コードを書かない。**
   依頼文のテンプレートが手引きにある。
2. **誤ったデータを出すくらいなら、空欄で出す。**
   空欄は「取れなかった」と分かるが、誤った値は正しい顔をして残る。
   値の意味が確認できていないうちは、機械的な読み替えをしない。
3. **実サイトで確かめられない性能改善を入れない。**
   前回入れた `pageLoadStrategy=eager` は実測すると効果が無く、それでいて
   不具合の切り分けを誤らせた。速度は実測で、現実的な規模で測る。
   あわせて、**確かめていないことは確かめていないと書く**（コメントにも）。

## kaigo_kensaku を触るとき

- 設計の背景と、サイト側の癖は `kaigo_kensaku/README.md` に書いてある。
  「なぜそう作ったか」まで書いてあるので、直す前に読むこと
- テストは `cd kaigo_kensaku && python -m unittest discover -s tests`。
  Chrome が無い環境では、ブラウザを使うテストは自動でスキップされる
- 取得ロジックを変えたら `src/state.py` の `DATA_VERSION` を上げる。
  古い進捗データが混ざるのを防ぐため
- 模擬サイト `tests/mocksite.py` は実サイトの癖を再現したもの。
  新しい癖が分かったら、**コードより先に模擬サイトへ反映**し、
  修正前のコードでテストが失敗することを確認する

## 共通

- 利用者は介護事業者で、PC操作を専門としない。
  エラーは「何をすればよいか」まで日本語で示すこと
- コメントは「何をしているか」ではなく「なぜそうしたか」を書く
